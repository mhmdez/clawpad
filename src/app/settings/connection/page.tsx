"use client";

import { GatewayConnectionSettings } from "@/components/settings/gateway-connection-settings";

export default function ConnectionSettingsPage() {
  return <GatewayConnectionSettings />;
}
